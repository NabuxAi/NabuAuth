# NabuAuth — راهنمای Claude

## پروژه چیست
NabuAuth **هویتِ مرکزیِ** اکوسیستم Nabu است: یک سرورِ OAuth 2.0 / OpenID Connect
که همهٔ محصول‌ها (NabuDesk، NabuGen، NabuGate، NabuVoice، NabuWrite، NabuSu،
NabuWatch، Nabux Store) کاربر را با آن وارد می‌کنند، به‌علاوهٔ **کیفِ پولِ
پیش‌پرداختِ مشترک** که همان محصول‌ها از آن خرج می‌کنند. یک حساب در
`auth.nabuxai.com` برای همهٔ آن‌ها.

## فناوری
- **زبان:** Go 1.24 (stdlib + `pgx` برای Postgres + `x/crypto/bcrypt` + `yaml.v3`).
- **اجرا:** باینریِ استاتیک، ایمیجِ distroless، پورت `8099`.
- **ذخیره‌سازی:** Postgres. اسکیمای `internal/store/schema.sql` در هر بوت اجرا
  می‌شود و همهٔ دستوراتش idempotent‌اند، پس ری‌دیپلوی روی دیتابیسِ موجود بی‌اثر است.

## ساختار
```
cmd/nabuauth/main.go     # نقطهٔ شروع: config → store → keyring → server
internal/config/         # خواندن apps.yaml و اعتبارسنجی
internal/store/          # اسکیما و همهٔ کوئری‌ها (کاربر، کلاینت، کد، توکن، کیف پول)
internal/tokens/         # امضا/راستی‌آزماییِ JWT با RS256، JWKS، PKCE، توکنِ opaque
internal/server/         # HTTP: oauth.go، api.go، wallet.go، ui.go
web/                     # قالب‌های embed شده + CSS
apps.yaml                # رجیستریِ اپ‌های اکوسیستم (بدونِ سکرت)
apps.local.yaml          # همان برای توسعهٔ محلی
```

## قراردادِ مهم
- **`apps.yaml` منبعِ حقیقت است، نه داشبورد.** کلاینت‌ها در بوت upsert می‌شوند.
  سکرت هرگز در فایل نیست؛ هر اپ نامِ یک env var می‌دهد (`secret_env`).
- **`/api/v1/user` را نشکن.** SDKهای منتشرشدهٔ NabuGate همین مسیر و همین شکلِ
  پاسخ را صدا می‌زنند. `/oauth/userinfo` نامِ استانداردِ همان است.
- **ایمیل و تلفن فقط با اسکوپِ خودشان برمی‌گردند.** توکنی که فقط `profile` دارد
  نباید ایمیل ببیند.
- **`redirect_uri` تطبیقِ دقیق است، نه پیشوندی.** تطبیقِ پیشوندی همان چیزی است که
  open redirect می‌سازد. تا وقتی `redirect_uri` تأیید نشده، هیچ خطایی redirect
  نمی‌شود — صفحهٔ خطا رندر می‌شود.
- **کلاینتِ public اجباراً PKCE.** بدون سکرت، PKCE تنها چیزی است که بین کدِ دزدیده‌شده
  و توکن می‌ایستد.
- **refresh token می‌چرخد.** مصرفِ دوباره رد می‌شود؛ همین است که سرقت را آشکار می‌کند.
- **`alg` در هدرِ JWT پین شده روی RS256.** اعتماد به `alg`ِ خودِ توکن کلاسیک‌ترین
  حفرهٔ JWT است. تستِ `TestVerifyRejectsAlgNone` نگهبانِ همین است.
- **debit باید idempotent باشد.** NabuGate برای هر درخواست یک کلید ثابت می‌فرستد؛
  ایندکسِ یکتا تلاشِ دوباره را به یک lookup تبدیل می‌کند. بدونِ این، هر retry
  مشتری را دوباره شارژ می‌کند.
- **`amount_cents` علامت‌دار است** (بدهکار منفی، بستانکار مثبت) تا دفتر با موجودی
  جمع بخورد و علامتِ اشتباه به‌جای شارژِ خاموش، در مغایرت‌گیری دیده شود.
- **واحدِ خرد به ارز وابسته است.** ریالِ عمانی (OMR) هزار بیسه دارد نه صد؛ نمایش
  با دو رقمِ اعشار همهٔ موجودی‌ها را ده برابر اشتباه نشان می‌دهد
  (`minorUnitDigits`).
- **کلیدِ امضا در دیتابیس است، نه env.** دیپلویِ تازه بدونِ مراسمِ کلید توکنِ معتبر
  می‌دهد و همهٔ replicaها با یک کلید امضا می‌کنند.
- **اولین حسابِ ساخته‌شده ادمین است** و فرمِ ثبت‌نام حتی وقتی بسته است تا وقتی
  دیتابیس هیچ کاربری ندارد باز می‌ماند، تا دیپلویِ تازه صاحب پیدا کند.

## دستورات
```bash
go build ./...            # ساخت
go vet ./...              # بررسی ایستا
go test ./...             # تست‌ها (تست‌های یکپارچه بدونِ دیتابیس skip می‌شوند)

createdb nabuauth_test
NABUAUTH_TEST_DATABASE_URL="postgres://localhost/nabuauth_test?sslmode=disable" go test ./...

createdb nabuauth_dev
DATABASE_URL="postgres://localhost/nabuauth_dev?sslmode=disable" \
  go run ./cmd/nabuauth -config apps.local.yaml
```

## افزودنِ اپ به اکوسیستم
بدونِ تغییرِ کد: یک بلوک در `apps.yaml` با `redirect_uris` و اسکوپ‌ها اضافه کن،
سکرت بساز (`openssl rand -base64 32`)، env var را روی هر دو سمت بگذار، ری‌دیپلوی کن.

## سبک کد و PR
- دیفِ کوچک و کم‌ریسک؛ سازگاریِ OAuth/OIDC را نشکن.
- برای هر تغییرِ رفتاری تست اضافه/به‌روز کن (`httptest` + دیتابیسِ تست).
- پیامِ کامیت/PR/کد بدونِ شناسهٔ مدل یا اطلاعاتِ داخلیِ ابزار.
